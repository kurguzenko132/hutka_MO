# Step 39 build fix 3

Исправлена TypeScript-ошибка в `lib/dashboard.ts`: тип `OverviewRow` теперь содержит `hot_contacts`, а dashboard использует безопасный `hotCount`.

Supabase обновлять не нужно.
